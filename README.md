# Prebuilt nvim binaries

## Quick Start 

```shell
# Github
curl -fsSL https://github.com/mtdcy/nvim-build/releases/latest/download/$(uname -m)-$OSTYPE.tar.gz | tar -C /opt -xz
# CN
curl -fsSL https://gitea.mtdcy.top/mtdcy/nvim-build/releases/download/latest/$(uname -m)-$OSTYPE.tar.gz | tar -C /opt -xz

/opt/nvim --version
```
